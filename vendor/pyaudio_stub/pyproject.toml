[project]
name = "pyaudio"
version = "0.2.14"
description = "Local stub used to satisfy Fish Speech's optional pyaudio dependency."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
